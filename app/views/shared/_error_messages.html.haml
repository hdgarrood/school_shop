- if target.errors.any?
  #errorExplanation
    %h4
      = pluralize(target.errors.count, 'error')
      prevented this
      = target.class.model_name.human.downcase
      from being saved:
    %ul
      - target.errors.full_messages.each do |msg|
        %li= msg
